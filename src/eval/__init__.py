"""
Eval module for offline prompt scoring.

Provides:
  - models    — Pydantic models for eval test cases and per-case results
  - metrics   — Individual scoring functions (format, hallucination, equivalence, etc.)
  - prompt_evaluator — Main PromptEvaluator orchestrator class
"""
